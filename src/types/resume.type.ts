export type Resume = {
  collectionId: string;
  collectionName: string;
  created: string; // ISO date string
  document: string;
  id: string;
  is_active: boolean;
  title: string;
  updated: string; // ISO date string
};
